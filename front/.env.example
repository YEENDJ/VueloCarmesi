NEXT_PUBLIC_API_URL=http://localhost:3001

# Dominio público del sitio, con esquema y sin barra final.
# Solo hace falta ponerla para un despliegue de PRUEBA en otro dominio: ahí el
# canonical debe apuntarse a sí mismo y no a producción, para que las dos copias
# no compitan en el índice de Google.
# Vacía o ausente, el código usa https://vuelocarmesi.com, que es lo correcto
# tanto en producción como en local.
NEXT_PUBLIC_SITE_URL=
